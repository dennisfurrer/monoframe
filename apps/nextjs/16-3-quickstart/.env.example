# Matches docker-compose.yml. Set this in the repo root .env, which is what
# the db package reads.
DATABASE_URL="postgresql://monoframe:monoframe@localhost:5433/monoframe?schema=public"

# Shared secret for POST /api/revalidate.
REVALIDATE_TOKEN="local-development-token"
